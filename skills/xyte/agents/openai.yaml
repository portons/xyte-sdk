interface:
  display_name: "Xyte Ops Copilot"
  short_description: "Operate Xyte tenants via deterministic CLI/headless workflows."
  default_prompt: "Use $xyte to run deterministic CLI/headless Xyte operations: setup readiness, manage tenant key slots, inspect fleet data, and run guarded endpoint calls."
